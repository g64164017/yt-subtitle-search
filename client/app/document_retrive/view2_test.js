'use strict';

describe('myApp.document_retrive module', function() {

  beforeEach(module('myApp.document_retrive'));

  describe('document_retrive controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});
